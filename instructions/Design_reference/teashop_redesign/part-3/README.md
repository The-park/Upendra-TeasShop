Part 3 — Menu, product inventory & product pages
------------------------------------------------

Goal: replace current menu and inventory pages with redesigned components.

Tasks:
- Create product card, product list, and filter components (Blade + CSS)
- Map data fields from `Product` and `Category` models to UI components
- Implement product inventory page (see attached `teashop_product_inventory` design)
- Verify server-side pagination, search and filters continue to work
- Add incremental deploy: enable new views behind a feature flag if needed

Acceptance criteria:
- Product pages render without errors and match design behavior
- Search/pagination/filter functionality unchanged or improved
